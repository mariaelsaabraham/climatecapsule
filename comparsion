<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>NASA Climate Change Time Capsule</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <style>
    body {
      background: radial-gradient(circle at top, #0c1326 0%, #060a14 100%);
      color: #e0e0e0;
      font-family: "Poppins", sans-serif;
      text-align: center;
      padding: 2rem;
      overflow-x: hidden;
    }

    h1 {
      color: #4fc3f7;
      font-size: 2.6rem;
      margin-bottom: 0.8rem;
      text-shadow: 0 0 12px rgba(79, 195, 247, 0.6);
    }

    p {
      color: #b0bec5;
      font-size: 1.05rem;
    }

    .search-box {
      margin: 2rem auto;
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 1rem;
      background: rgba(25, 35, 55, 0.8);
      padding: 1.5rem;
      border-radius: 16px;
      box-shadow: 0 4px 25px rgba(0, 0, 0, 0.4);
      backdrop-filter: blur(6px);
      max-width: 850px;
    }

    .search-box input {
      padding: 0.9rem 1rem;
      border-radius: 10px;
      border: none;
      background: #141a2b;
      color: #e0e0e0;
      outline: none;
      font-size: 1rem;
      min-width: 180px;
      box-shadow: inset 0 0 10px rgba(79, 195, 247, 0.1);
    }

    .search-box button {
      background: linear-gradient(90deg, #4fc3f7, #82e9de);
      border: none;
      border-radius: 10px;
      padding: 0.9rem 1.5rem;
      cursor: pointer;
      color: #0a0f1b;
      font-weight: 700;
      letter-spacing: 0.5px;
      transition: 0.3s;
      box-shadow: 0 0 12px rgba(79, 195, 247, 0.4);
    }

    .search-box button:hover {
      transform: scale(1.05);
      box-shadow: 0 0 20px rgba(79, 195, 247, 0.7);
    }

    .comparison {
      display: none;
      margin-top: 2.5rem;
      animation: fadeIn 0.8s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .image-grid {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 2.5rem;
    }

    .year-card {
      background: rgba(18, 24, 39, 0.95);
      border-radius: 18px;
      overflow: hidden;
      width: 380px;
      box-shadow: 0 6px 25px rgba(0, 0, 0, 0.5);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .year-card:hover {
      transform: translateY(-6px);
      box-shadow: 0 10px 35px rgba(79, 195, 247, 0.3);
    }

    .year-card img {
      width: 100%;
      height: 250px;
      object-fit: cover;
      background: #111;
      border-bottom: 2px solid rgba(79, 195, 247, 0.3);
    }

    .year-card h2 {
      color: #4fc3f7;
      font-size: 1.5rem;
      margin-top: 1rem;
    }

    .desc {
      color: #b0bec5;
      font-size: 0.95rem;
      padding: 0.8rem 1.2rem 1.5rem;
      line-height: 1.6;
    }

    .info {
      max-width: 750px;
      margin: 2rem auto;
      color: #cfd8dc;
      line-height: 1.8;
      font-size: 1.05rem;
      background: rgba(25, 35, 55, 0.6);
      border-radius: 12px;
      padding: 1.5rem;
      box-shadow: inset 0 0 20px rgba(79, 195, 247, 0.1);
    }

    footer {
      margin-top: 3rem;
      font-size: 0.9rem;
      color: #78909c;
    }
  </style>
</head>
<body>
  <h1><i class="fa-solid fa-earth-americas"></i> NASA Climate Change Time Capsule</h1>
  <p>Compare NASA satellite imagery of any location between years 2000 to 2025.</p>

  <div class="search-box">
    <input type="text" id="location" placeholder="Enter a location (e.g., Dubai, Greenland)" />
    <input type="number" id="year1" placeholder="Enter first year (e.g., 2000)" min="1984" max="2025" />
    <input type="number" id="year2" placeholder="Enter second year (e.g., 2025)" min="1984" max="2025" />
    <button id="compareBtn"><i class="fa-solid fa-magnifying-glass"></i> Compare</button>
  </div>

  <div id="comparison" class="comparison">
    <div class="image-grid">
      <div class="year-card">
        <h2 id="label1"><i class="fa-solid fa-calendar-days"></i></h2>
        <img id="img1" src="" alt="Image for Year 1" />
        <div class="desc" id="desc1"></div>
      </div>
      <div class="year-card">
        <h2 id="label2"><i class="fa-solid fa-calendar-days"></i></h2>
        <img id="img2" src="" alt="Image for Year 2" />
        <div class="desc" id="desc2"></div>
      </div>
    </div>

    <div class="info" id="info"></div>
  </div>

  <footer>
    <p>Data Source: NASA GIBS (MODIS Terra) | Built for Educational Exploration 🌍</p>
  </footer>

  <script>
    document.getElementById("compareBtn").addEventListener("click", async () => {
      const location = document.getElementById("location").value.trim();
      const year1 = document.getElementById("year1").value;
      const year2 = document.getElementById("year2").value;

      if (!location || !year1 || !year2) {
        alert("Please fill all fields: location and both years.");
        return;
      }

      const geoRes = await fetch(https://nominatim.openstreetmap.org/search?format=json&q=${location});
      const geoData = await geoRes.json();

      if (!geoData.length) {
        alert("Location not found. Please try another.");
        return;
      }

      const lat = parseFloat(geoData[0].lat);
      const lon = parseFloat(geoData[0].lon);
      const bboxSize = 2; // degrees around location
      const bbox = ${lon - bboxSize},${lat - bboxSize},${lon + bboxSize},${lat + bboxSize};
      const baseUrl = "https://gibs.earthdata.nasa.gov/wms/epsg4326/best/wms.cgi";
      const layer = "MODIS_Terra_CorrectedReflectance_TrueColor";

      const url1 = ${baseUrl}?SERVICE=WMS&REQUEST=GetMap&VERSION=1.3.0&LAYERS=${layer}&STYLES=&FORMAT=image/jpeg&CRS=EPSG:4326&BBOX=${bbox}&WIDTH=800&HEIGHT=600&TIME=${year1}-07-01;
      const url2 = ${baseUrl}?SERVICE=WMS&REQUEST=GetMap&VERSION=1.3.0&LAYERS=${layer}&STYLES=&FORMAT=image/jpeg&CRS=EPSG:4326&BBOX=${bbox}&WIDTH=800&HEIGHT=600&TIME=${year2}-07-01;

      document.getElementById("label1").innerHTML = <i class="fa-solid fa-calendar-days"></i> Year ${year1};
      document.getElementById("label2").innerHTML = <i class="fa-solid fa-calendar-days"></i> Year ${year2};
      document.getElementById("img1").src = url1;
      document.getElementById("img2").src = url2;
      document.getElementById("desc1").innerText = This image of ${location} from ${year1} shows the landscape and environmental state during that period.;
      document.getElementById("desc2").innerText = By ${year2}, satellite imagery of ${location} reveals visible changes—urban growth, reduced vegetation, or melting ice.;
      document.getElementById("info").innerText = Comparing ${location} between ${year1} and ${year2} provides a glimpse into how Earth's climate and human activity have shaped this region over time.;

      document.getElementById("comparison").style.display = "block";
    });
  </script>
</body>
</html>
